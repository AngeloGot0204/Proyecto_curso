[project]
name = "proyecto-curso"
version = "0.1.0"
requires-python = ">=3.12"
dependencies = [
    "Django>=5.2.8,<6.0",
    "psycopg[binary]>=3.1.12,<4",
    "python-dotenv>=1.0,<2",
    "dj-database-url>=2.2,<3",
]

[tool.vercel]
entrypoint = "config.wsgi:application"
