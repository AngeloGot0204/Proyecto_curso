[pytest]
DJANGO_SETTINGS_MODULE = config.settings
python_files = test_*.py
addopts = --reuse-db
norecursedirs = .venv .git .pytest_cache
markers =
    modelo: Adjunto model-only tests (backlog #11, tasks.md Unit 1 focused test command)
